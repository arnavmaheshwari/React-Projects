import React from 'react';
import ReactDOM from 'react-dom/client';
import MoviesList from './CompletedProjects/MoviesList.js';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <MoviesList />
);